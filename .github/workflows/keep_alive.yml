name: Keep Render Awake
on:
  schedule:
    - cron: '*/10 * * * *' # Runs every 10 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -s https://informal-worker.onrender.com/ > /dev/null